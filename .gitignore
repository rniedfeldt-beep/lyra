# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Copyrighted source PDFs — never read at runtime, never committed (rule 2).
# /data (creatures, templates, etc.) IS tracked: it's the clean, offline-
# extracted output the app actually consumes, per the Directory Shape spec.
/sourcebooks/
*.bak

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
